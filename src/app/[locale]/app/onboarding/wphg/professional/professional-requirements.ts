export type ProfessionalRequirements = {
  // if PersonType.Customer
  assets: boolean
  experience: boolean
  transactions: boolean
  // if PersonType.Company
  balance: boolean
  equity: boolean
  revenue: boolean
}
